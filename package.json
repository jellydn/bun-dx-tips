{
  "name": "bun-tips",
  "type": "module",
  "module": "server.ts",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --hot run server.ts",
    "dev:inspect": "bun --inspect server.ts",
    "dev:watch": "bun run --watch server.ts"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@productsway/logger": "workspace:*",
    "@types/express": "^4.17.18",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
